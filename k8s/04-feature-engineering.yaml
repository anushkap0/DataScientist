apiVersion: apps/v1
kind: Deployment
metadata:
  name: feature-engineering
  namespace: autonomous-ds
spec:
  replicas: 2
  selector:
    matchLabels: { app: feature-engineering }
  template:
    metadata:
      labels: { app: feature-engineering }
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8000"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: feature-engineering
          image: REGISTRY/ads-feature-engineering:latest   # set by scripts/deploy.sh
          ports: [{ containerPort: 8000 }]
          envFrom:
            - configMapRef: { name: ads-config }
          env:
            - name: MINIO_ACCESS_KEY
              valueFrom: { secretKeyRef: { name: minio-credentials, key: access-key } }
            - name: MINIO_SECRET_KEY
              valueFrom: { secretKeyRef: { name: minio-credentials, key: secret-key } }
          resources:
            requests: { cpu: "250m", memory: "256Mi" }
            limits: { cpu: "1", memory: "1Gi" }
          readinessProbe:
            httpGet: { path: /health, port: 8000 }
            initialDelaySeconds: 5
          livenessProbe:
            httpGet: { path: /health, port: 8000 }
            initialDelaySeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: feature-engineering-svc
  namespace: autonomous-ds
spec:
  selector: { app: feature-engineering }
  ports: [{ port: 8000, targetPort: 8000 }]
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: feature-engineering-hpa
  namespace: autonomous-ds
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: feature-engineering
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 70 }
